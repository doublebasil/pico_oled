Fix the damn font code its buggered

Add support for writing multiple characters

Don't let the user set pixels outside of the display boundaries

Find the size of the fonttables and write it in the header files

Add image support

Add QR generation

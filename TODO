Make sure all the bitmap setting functions don't set values outside of the 
calloc boundary. Also make sure we aren't using any negative indexes for the
bitmap pointers

Add QR generation

Move the loading circle bitmap pushing code out into it's own module
scope function so...
Add a loading circle colour changing function, which using the loading circle
bitmap pushing function?

Add the border thing to the horizontal loading bar. The border should be
drawn in the init function and should be untouched by the display function
Add border to loading circle? Or just don't

Neaten the oled_test function, make the gradients use the display dimensions
in the calculations

